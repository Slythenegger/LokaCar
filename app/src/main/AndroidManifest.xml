<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.stropee2017.lokacar">

    <application
        android:name=".beans.Agence"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">

        </activity>
        <activity android:name=".ClientActivity" />
        <activity android:name=".AddVoitureActivity" />
        <activity android:name=".AddClientActivity" />
        <activity android:name=".DetailClientActivity" />
        <activity android:name=".AccueilActivity"/>
        <activity android:name=".EtatBusinessActivity"/>

        <activity android:name=".VoitureActivity" />
        <activity android:name=".DetailVoitureActivity" />
        <activity android:name=".LocationActivity" />
        <activity android:name=".LocationEnCoursActivity" />
        <activity android:name=".ListeLocEnCoursActivity"></activity>
    </application>

</manifest>