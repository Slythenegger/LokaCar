<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <gradient
        android:centerColor="@color/colorMidClient"
        android:endColor="@color/colorlocEnCours"
        android:startColor="@color/colorPrimary"></gradient>


    <!-- bordure -->
    <stroke
        android:width="3dp"
        android:color="@color/colorPrimaryDark" />

    <padding
        android:bottom="5dp"
        android:left="10dp"
        android:right="10dp"
        android:top="5dp" />

    <!-- border radius -->
    <corners android:radius="10dp" />

</shape>